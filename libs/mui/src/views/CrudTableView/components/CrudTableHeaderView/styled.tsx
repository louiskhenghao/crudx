import Box from '@mui/material/Box';
import { styled } from '@mui/material/styles';

// Styled online status
export const StyledBox = styled(Box)(() => {
  return {
    paddingTop: '0.5rem',
    paddingBottom: '0.5rem',
  };
});
